

mycat(){
  echo "Just echo back stdin:"
  cat
}

echo "Content-type: text/plain"
echo
POST="$(cat | head -n3)"
SCR=$(echo -n "$POST" | head -n1)
STDIN=$(echo -n "$POST" | tail -n1) 
STDOUT=$(echo -n "$STDIN" | base64 -d | eval "$SCR" | base64 | tr -d '\n')
echo $?
echo h2
echo -n "$STDOUT"
