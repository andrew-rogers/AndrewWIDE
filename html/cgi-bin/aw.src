

mycat(){
  echo "Just echo back stdin:"
  cat
}

echo "Content-type: text/plain"
echo
POST="$(cat)"
SCR=$(echo -n "$POST" | head -n1)
STDIN=$(echo -n "$POST" | tail -n +3) 
STDOUT=$(echo -n "$STDIN" | eval "$SCR")
echo $?
echo h2
echo -n "$STDOUT"
